Basic renderer, loads scenes from gltf files and displays them on an interactable window with a camera.

Uses both draw indirect and classic draw command recording.

Does frustum culling for both pipelines.
